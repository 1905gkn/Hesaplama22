import fs from "node:fs";

const file = "dist/server/index.js";
let source = fs.readFileSync(file, "utf8");
const match = source.match(/const\s+HTML_BASE64\s*=\s*(["'])([A-Za-z0-9+/=]+)\1/);
if (!match) throw new Error("HTML_BASE64 not found for free block counts v2");
let html = Buffer.from(match[2], "base64").toString("utf8");
html = html
  .replace(/<style data-rafex-free-block-counts="v2">[\s\S]*?<\/style>/g, "")
  .replace(/<script data-rafex-free-block-counts="v2">[\s\S]*?<\/script>/g, "");
const runtime = "\n<style data-rafex-free-block-counts=\"v2\">\n#m2LayoutProductList.rafex-block-counts-v2{display:block!important;padding:0!important;overflow:hidden!important;border:1px solid #cbd9d0!important;border-radius:10px!important;background:#fff!important}\n.rafex-block-count-title{display:block;padding:9px 11px;background:#173c2d;color:#fff;font:900 11px/1.2 Arial,sans-serif;letter-spacing:.04em}\n.rafex-block-count-empty{display:block;padding:12px;color:#68766e;font:800 10px/1.35 Arial,sans-serif}\n.rafex-block-count-group{display:block;border-top:1px solid #dfe8e2}\n.rafex-block-count-group>h4{margin:0;padding:8px 11px;background:#f3f7f4;color:#173c2d;font:900 10px/1.2 Arial,sans-serif}\n.rafex-block-count-row{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:12px;align-items:center;padding:7px 11px;border-top:1px solid #edf2ee}\n.rafex-block-count-row>span{min-width:0;display:flex;flex-direction:column;gap:2px;color:#263a2f;font:800 10px/1.25 Arial,sans-serif}\n.rafex-block-count-row small{color:#748078;font:700 8px/1.25 Arial,sans-serif}\n.rafex-block-count-row strong{color:#173c2d;font:900 11px/1.2 Arial,sans-serif;white-space:nowrap}\n</style>\n<script data-rafex-free-block-counts=\"v2\">\n(function(){\n  if(window.__rafexFreeBlockCountsV2)return;\n  window.__rafexFreeBlockCountsV2=true;\n  var pending=0,lastSignature='',layoutObserver=null,observedLayer=null;\n  var systemLabels={b2b:'B2B',mr:'MR',mekik2:'MEKİK',other:'YERLEŞİM'};\n  function esc(value){return String(value==null?'':value).replace(/[&<>\"]/g,function(ch){return {'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[ch]})}\n  function page(){return document.getElementById('page')}\n  function isCommon(){var p=page();return !!(p&&(p.dataset.rafexFreeDrawing==='1'||p.classList.contains('rafex-free-drawing-page')))}\n  function activeModule(){try{return typeof m2ActiveModule!=='undefined'?String(m2ActiveModule):''}catch(e){return''}}\n  function isB2B(){var p=page(),title=String(document.getElementById('pageTitle')?.textContent||'').trim().toUpperCase();return !!(p&&!isCommon()&&(p.classList.contains('b2b-mode')||activeModule()==='b2b'||title==='B2B'))}\n  function targetPage(){return isCommon()||isB2B()}\n  function racks(){try{return typeof m2LayoutState!=='undefined'&&Array.isArray(m2LayoutState.racks)?m2LayoutState.racks:[]}catch(e){return[]}}\n  function symbols(){try{return typeof m2LayoutSymbols!=='undefined'&&Array.isArray(m2LayoutSymbols)?m2LayoutSymbols:[]}catch(e){return[]}}\n  function systemOf(rack){\n    var d=rack&&rack.drawing||rack||{},raw=String(rack&&rack.rafexSystem||d.rafexSystem||'').toLowerCase();\n    if(raw==='b2b'||raw==='mr'||raw==='mekik2')return raw;\n    if(d.b2b&&d.b2b.mr===true||d.plan&&d.plan.mr===true||d.systemType==='mr')return'mr';\n    if(d.b2bLayout||d.b2b)return'b2b';\n    return'mekik2';\n  }\n  function reportLabels(){try{if(typeof m2ReportDictionary==='function')return m2ReportDictionary('tr')}catch(e){}return{unitEach:'adet',items:{}}}\n  function add(map,system,row){\n    var qty=Math.max(0,Number(row&&row.qty)||0);if(!qty)return;\n    var item=String(row.item||row.name||'Ürün'),spec=String(row.spec||''),unit=String(row.unit||'adet');\n    var key=system+'|'+item+'|'+spec+'|'+unit,current=map.get(key)||{system:system,item:item,spec:spec,unit:unit,qty:0};\n    current.qty+=qty;map.set(key,current);\n  }\n  function bomForRack(rack,labels){\n    try{\n      if(typeof m2CorporateBomRows!=='function')return[];\n      var rows=Math.max(1,Number(rack?.b2bLayout?.rowCount)||1);\n      var footTeamCount=rack?.b2bLayout?Math.max(1,2*rows-(rack.sharedFootWith?rows:0)):undefined;\n      return m2CorporateBomRows({name:rack.typeName||rack.name||'Raf',drawing:rack,rackCount:1,rackIds:[rack.id],footTeamCount:footTeamCount},labels)||[];\n    }catch(error){return[]}\n  }\n  function aggregate(){\n    var map=new Map(),labels=reportLabels(),list=racks();\n    list.forEach(function(rack){\n      var system=systemOf(rack),rows=bomForRack(rack,labels);\n      rows.forEach(function(row){add(map,system,row)});\n    });\n    try{\n      if(typeof m2LayoutProductRows==='function'){\n        m2LayoutProductRows().filter(function(row){return /koruma|deprem|kolon|kapı|forklift|insan|bariyer/i.test(String(row.name||''))}).forEach(function(row){add(map,isCommon()?'other':'b2b',{item:row.name,spec:'Serbest yerleşim',qty:row.qty,unit:'adet'})});\n      }\n    }catch(error){}\n    if(list.length&&!map.size){\n      try{if(typeof m2LayoutProductRows==='function')m2LayoutProductRows().forEach(function(row){add(map,isCommon()?'other':'b2b',{item:row.name,qty:row.qty,unit:'adet'})})}catch(error){}\n    }\n    return Array.from(map.values()).sort(function(a,b){\n      var order={b2b:0,mr:1,mekik2:2,other:3};\n      return (order[a.system]||0)-(order[b.system]||0)||a.item.localeCompare(b.item,'tr')||a.spec.localeCompare(b.spec,'tr');\n    });\n  }\n  function rowHtml(row){return '<div class=\"rafex-block-count-row\"><span><b>'+esc(row.item)+'</b>'+(row.spec?'<small>'+esc(row.spec)+'</small>':'')+'</span><strong>'+esc(Math.round(row.qty))+' '+esc(row.unit)+'</strong></div>'}\n  function render(force){\n    if(!targetPage())return false;\n    var host=document.getElementById('m2LayoutProductList');if(!host)return false;\n    var list=racks(),sig=list.map(function(r){return [r.id,r.rafexSystem||'',r.typeName||'',r.bays||0,r.levels||0,r.depth||0,r.loadedLevels||0,r.footType||'',r.footProfile||'',r.traverseRecommendation||'',r.hasExtra?1:0,r.systemType||'',r.sharedFootWith||'',(r.seismicBraces||[]).length,r.b2bLayout?.palletCount||0,r.b2bLayout?.rowCount||0,r.b2bLayout?.sectionWidth||0,r.b2bLayout?.frameDepth||0].join(':')}).join('|')+'#'+symbols().map(function(s){return [s.id,s.type,s.rackId||'',s.widthMm||0].join(':')}).join('|');\n    if(!force&&sig===lastSignature&&host.classList.contains('rafex-block-counts-v2'))return true;\n    lastSignature=sig;var rows=aggregate();\n    host.classList.add('rafex-block-counts-v2');\n    if(!list.length){host.innerHTML='<b class=\"rafex-block-count-title\">BLOK İÇERİK ADETLERİ</b><span class=\"rafex-block-count-empty\">Serbest alana henüz blok eklenmedi.</span>';return true}\n    var systems=isCommon()?['b2b','mr','mekik2','other']:['b2b','other'];\n    var groups=systems.map(function(system){var current=rows.filter(function(row){return row.system===system});if(!current.length)return'';return '<section class=\"rafex-block-count-group\" data-system=\"'+system+'\"><h4>'+systemLabels[system]+' · '+current.length+' ÜRÜN SATIRI</h4>'+current.map(rowHtml).join('')+'</section>'}).join('');\n    host.innerHTML='<b class=\"rafex-block-count-title\">BLOK İÇERİK ADETLERİ · '+list.length+' BLOK</b>'+(groups||'<span class=\"rafex-block-count-empty\">Bloklar eklendi; ürün reçetesi bulunamadı.</span>');\n    return true;\n  }\n  function schedule(force){if(pending)cancelAnimationFrame(pending);pending=requestAnimationFrame(function(){pending=0;bindLayer();render(force===true)})}\n  function refreshBurst(){schedule(true);[40,140,360].forEach(function(delay){setTimeout(function(){schedule(true)},delay)})}\n  function bindLayer(){\n    var layer=document.getElementById('m2LayoutContent');if(!layer||layer===observedLayer)return;\n    if(layoutObserver)layoutObserver.disconnect();observedLayer=layer;\n    layoutObserver=new MutationObserver(function(records){if(records.some(function(r){return r.addedNodes.length||r.removedNodes.length}))schedule(false)});\n    layoutObserver.observe(layer,{childList:true,subtree:true});\n  }\n  function wrapRender(){\n    try{\n      var original=window.m2RenderLayout;\n      if(typeof original!=='function'||original.__rafexBlockCountsV2)return;\n      var wrapped=function(){var result=original.apply(this,arguments);schedule(false);return result};\n      wrapped.__rafexBlockCountsV2=true;window.m2RenderLayout=wrapped;\n    }catch(error){}\n  }\n  function wrapMutation(name){\n    try{\n      var original=window[name];\n      if(typeof original!=='function'||original.__rafexBlockCountsMutationV2)return;\n      var wrapped=function(){var result=original.apply(this,arguments);if(result&&typeof result.then==='function')result.finally(refreshBurst);else refreshBurst();return result};\n      wrapped.__rafexBlockCountsMutationV2=true;window[name]=wrapped;\n    }catch(error){}\n  }\n  function bindMutationHooks(){\n    wrapRender();\n    ['m2AddRack','m2AddSelectedSavedRack','m2DuplicateRack','m2CommitAutoFillGuide','m2DeleteRack','m2ClearLayout','m2ChooseJoinRack','m2SeparateSelectedRack','m2UndoLastAction','m2AddSeismicBrace','m2CopySymbol','m2LoadProject','m2HandleSavedRackTypeClick'].forEach(wrapMutation);\n  }\n  document.addEventListener('click',function(event){\n    var hit=event.target?.closest?.('button,[data-page]'),action=String(hit?.getAttribute?.('onclick')||'');\n    if(hit?.matches?.('[data-page]')||/m2(?:AddRack|AddSelectedSavedRack|DuplicateRack|CommitAutoFillGuide|DeleteRack|ClearLayout|ChooseJoinRack|SeparateSelectedRack|UndoLastAction|AddSeismicBrace|CopySymbol|LoadProject|HandleSavedRackTypeClick)/.test(action)){\n      setTimeout(function(){bindMutationHooks();bindLayer();refreshBurst()},0);\n    }\n  },true);\n  window.addEventListener('hashchange',function(){setTimeout(function(){bindLayer();schedule(true)},0)});\n  window.rafexRefreshFreeBlockCountsV2=function(){schedule(true)};\n  function boot(){bindMutationHooks();bindLayer();schedule(true)}\n  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();\n})();\n</script>";
const close = html.lastIndexOf("</body>");
if (close < 0) throw new Error("body close missing for free block counts v2");
html = html.slice(0, close) + runtime + "\n" + html.slice(close);
for (const required of ["data-rafex-free-block-counts=\"v2\"", "__rafexFreeBlockCountsV2", "BLOK İÇERİK ADETLERİ", "rafexRefreshFreeBlockCountsV2"]) {
  if (!html.includes(required)) throw new Error("Free block counts v2 missing: " + required);
}
const encoded = Buffer.from(html).toString("base64");
source = source.slice(0, match.index) + match[0].replace(match[2], encoded) + source.slice(match.index + match[0].length);
fs.writeFileSync(file, source);
console.log("Free block counts v2: B2B and common drawing counts follow placed blocks.");
